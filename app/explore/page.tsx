import BigTitle from "@/components/BigTitle";
import MiniTitle from "@/components/MiniTitle";

const ExplorePage = () => {
  return (
    <div className="py-32 px-4 w-full flex justify-center items-center">
      <BigTitle>Select a type of content</BigTitle>
    </div>
  );
};

export default ExplorePage;
